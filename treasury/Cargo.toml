[package]
name = "treasury_contract"
version = "0.1.3"

edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "25.1.0"

[dev-dependencies]
soroban-sdk = { version = "25.1.0", features = ["testutils"] }

[features]
default = ["testnet"]
testnet = []
mainnet = []